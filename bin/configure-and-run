#!/bin/sh

if [ -z "$PORT" ]; then
   echo "Missing $PORT" >&2
   exit 1
fi

echo "port $PORT" > etc/runtime.conf

if [ -n "$REDIS_PASSWORD" ]; then
    echo "requirepass $REDIS_PASSWORD" >> etc/runtime.conf
fi

exec "$@"
